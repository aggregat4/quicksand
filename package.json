{
  "name": "quicksand-e2e",
  "private": true,
  "scripts": {
    "pretest:e2e": "rm -rf target/e2e-db && mvn -DskipTests package",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
